name: Hello Agile

on:
  push:
    branches: [ dev ]          # Runs when you push to the dev branch
  pull_request:
    branches: [ main ]         # Runs when you open or update a PR to main

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello from GitHub Actions!"
